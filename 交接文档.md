# FlSQLite Viewer 交接文档

## 1. 项目定位

`FlSQLite Viewer` 是一个跨平台 SQLite 数据库查看与编辑器，支持 Android 和 Windows。

核心能力：
- 打开本地 `.db/.sqlite` 文件，浏览表结构，分页查询，搜索，新增/编辑/删除行
- Android 支持四种文件访问通道：普通文件访问、全部文件访问权限（MANAGE_EXTERNAL_STORAGE）、Root（su）、Shizuku

## 2. 项目框架

```text
D:\Flutter_Projucts\FlSQLite_Viewer\
  lib/
    main.dart                          # 应用入口
    app/app.dart                       # MaterialApp、主题、ProviderScope
    core/
      data/
        sqlite_database_service.dart   # 底层 SQLite 打开/关闭/查询/CRUD
        sqlite_repository.dart         # 仓库层，模型转换和安全包装
      file_session/
        database_file_access.dart       # 文件访问抽象（普通/工作副本/WAL处理）
        android_special_file_access.dart # Android 平台通道：Root/Shizuku/全文件
    models/
      database_models.dart              # TableColumnInfo、DatabaseTable、TablePage 等
    state/
      database_controller.dart          # 数据库会话、当前表、分页、搜索、CRUD 状态
      file_access_controller.dart       # Android 权限开关、状态检测、文件操作
      file_browser_controller.dart      # 文件浏览器：当前路径、目录列表、过滤、测试路径
    features/
      android_access/presentation/      # Android 高级访问设置页
      file_browser/presentation/        # 内置文件浏览器页
      database/presentation/            # 数据表（DataGrid）和行编辑弹窗（RowEditorDialog）
    shared/widgets/                     # 通用控件（AccessSwitchTile、FileEntryTile、PathBreadcrumbs）
    ui/home_page.dart                   # 主页面布局入口
  android/app/src/main/kotlin/lingxue/flsqliteviewer/
    MainActivity.kt                     # 平台通道入口（storage_access channel）
    fileaccess/
      RootFileAccess.kt                 # Root 文件检测/读写/目录枚举/权限保持
      ShizukuFileAccess.kt              # Shizuku 文件读写/目录枚举
  test/                                 # 单元和 widget 测试
  交接文档.md                           # 本文件
```

## 3. 核心依赖

- `sqlite3` / `sqlite3_flutter_libs`：SQLite 访问
- `flutter_riverpod`：状态管理
- `file_picker`：文件选择
- `path_provider`：应用数据目录
- `shared_preferences`：持久化开关
- `package:collection`：集合工具
- Android 原生：Shizuku API (`dev.rikka.shizuku:api:13.1.5`, `dev.rikka.shizuku:provider:13.1.5`)

## 4. 包名与命名空间

全项目统一使用 `lingxue.flsqliteviewer`：
- `pubspec.yaml` name：`flsqliteviewer`
- Android `namespace` / `applicationId`：`lingxue.flsqliteviewer`
- Kotlin 源码路径：`android/app/src/main/kotlin/lingxue/flsqliteviewer/`

## 5. Android 文件访问模型

所有特殊路径（Root/Shizuku）不直接用 `sqlite3` 打开，而是：
1. 通过对应通道把源文件复制到应用私有工作区
2. 在工作副本上做所有 SQLite 操作
3. 保存时通过同一通道写回源路径，同时同步 WAL/SHM 侧车文件

四种通道：
| 通道 | 适用场景 | 权限/前提 |
|------|----------|-----------|
| 普通 | 外部存储目录 | 默认 |
| 全部文件访问 | `/storage/emulated/0/` 下普通目录 | MANAGE_EXTERNAL_STORAGE |
| Root | `/data/user/0/`、`/data/data/` | 设备已 root |
| Shizuku | `/storage/emulated/0/Android/data/` | Shizuku 安装并授权 |

内置测试路径：
- Root：`/data/user/0/`
- Shizuku：`/storage/emulated/0/Android/data/`
- 普通：`/storage/emulated/0/`

## 6. 当前版本状态

### V1.0.1 Android 特权目录访问修复

提交：`b7687ee V1.0.1 修复 Android 特权目录访问`

本版本修复重点：
- Root 模式启用后，对 `/data/user/0`、`/data/data`、`/storage`、`/sdcard` 等路径优先或兜底走 Root 通道。
- 受限路径不再错误回退到普通文件访问，避免出现 `shizuku → normal` 或 `normal` 权限错误链。
- Root 原生目录枚举从 `find -exec sh` 改为 shell glob 遍历，降低 Android/toybox 环境兼容问题。
- Root 候选路径补齐 `/data/user/0` ↔ `/data/data`、`/storage/emulated/0` ↔ `/sdcard` 的目录本身映射。
- 平台通道错误会透出候选路径、exit code、stderr 和堆栈，便于区分代码问题与设备授权问题。
- Shizuku 设置页按钮改为真正请求 Shizuku 授权，并同步刷新授权状态。
- 普通目录访问对不存在或不可访问目录改为抛出明确异常，不再用空列表掩盖失败。

验证记录：
- `flutter analyze`：通过，`No issues found!`
- `flutter build apk --debug`：通过
- 真机：`f294cd23 / 22127RK46C / Android 14 (API 34)`，已安装 debug APK 并启动验证
- `/storage/emulated/0`：可正常列出内容
- `Root 测试路径`：模式链已修正为 `Tried modes: root`
- 当前设备对 `/data/user/0/com.deepseek.chat/download` 与 `/data/data/com.deepseek.chat/download` 返回 `exit=13 err=Permission denied`，表示 Root 管理器或系统策略仍拒绝该 app 的 su 访问；代码侧已经能正确走 Root 通道并显示真实失败原因

后续排查时优先确认：
1. Root 管理器中 `lingxue.flsqliteviewer` 是否已授权。
2. 设备上的 su 实现是否允许普通应用通过 `su -c` 访问 `/data/user/0`。
3. 若 Root 已授权但仍 `Permission denied`，先用同一 app 包名触发一次 Root 授权，再点文件浏览器中的“重试”。

## 7. 当前版本增量状态

### V1.0.2 文件浏览 Root 回退与数据库过滤

提交：`ca89dae V1.0.2 优化 Root 文件浏览与数据库过滤`

本次改动重点：
- `FileAccessController` 重新梳理路径候选模式顺序：
  - `/data`、`/system` 等受保护路径优先走 Root
  - `/storage`、`/sdcard` 普通目录优先普通/全部文件访问，再允许 Root 兜底
  - `forcedMode: normal` 不再把 Root 兜底链路截断
- `RootFileAccess` 目录枚举改为更稳的单次 shell 脚本，增加 `ls`/`find` 回退、分阶段错误摘要和更合适的超时。
- 文件浏览器新增“仅显示数据库”开关：开启后保留目录导航，仅显示目录与 `.db/.sqlite/.sqlite3` 文件。
- 文件浏览错误页改为摘要 + 可展开详情，避免长错误文本直接撑爆页面。
- 进入新目录成功后会自动清空上一层搜索词，避免像 `pica` 这类包名过滤继续作用到子目录，导致 `cache/files/databases` 等真实内容被误判为“空目录”。

本次验证记录：
- `flutter analyze`：通过，`No issues found!`
- `flutter build apk --debug`：通过
- 真机：`f294cd23 / 22127RK46C / Android 14 (API 34)`
- Android 高级访问页：`Root 模式 / Root 可用 / checked=true`
- `/storage/emulated/0/1/.1临时文件`：已能列出多项内容，不再只显示 `luluka`；真机抓到的可见项包括 `luluka`、`1.txt`、`1327951.zip`、`26.03 叶瞬光.rar`、`download.db`、`download.zip` 等
- “仅显示数据库”开关：开启后当前目录仅保留 `luluka` 目录与 `download.db`，普通文件被隐藏，目录导航保留正常
- `Root 测试路径`：可进入 `/data/user/0/com.deepseek.chat/download`，未再出现普通模式误兜底
- `/data/user/0/com.deepseek.chat`：真机可列出 `app_textures`、`app_webview`、`cache`、`code_cache`、`databases`、`download`、`files`、`no_backup`、`shared_prefs` 等子目录
- `/data/user/0`：真机可稳定列出应用包目录，抓到的可见项包括 `air.futaojp`、`android`、`com.android.settings`、`com.deepseek.chat`、`com.example.picakeep`、`com.google.android.gms`、`com.miui.securitycenter` 等
- `com.github.pacalini.pica_comic` 这类“先在 `/data/user/0` 搜包名，再点进包目录”的场景，根因已确认是搜索词在目录跳转后仍继续过滤子目录；本次已改为成功跳转后自动清空搜索词，并在空列表时优先提示“当前筛选无匹配项”
- 真机复测确认：当前已可进入 `/data/user/0/com.github.pacalini.pica_comic`，且在“仅显示数据库”开启时仍可列出目录项；当前抓到的可见子目录包括 `app_flutter`、`app_textures`、`app_webview`、`cache`、`code_cache`、`databases`、`files`、`no_backup`、`shared_prefs`

后续若继续补最后一项包级验证，建议直接：
1. 在 `/data/user/0` 页增加临时“输入绝对路径跳转”调试入口，或增加 `pica_comic` 专用测试路径
2. 或在当前真机上改用更小步长滚动/人工点击 `com.github.pacalini.pica_comic` 后再补一次抓屏

### V1.0.3 目录跳转后残留过滤修复

提交：`8e61097 V1.0.3 修复目录跳转后的残留过滤`

本次改动重点：
- 进入新目录成功后自动清空上一层搜索词，避免包名过滤继续作用到子目录。
- 页面搜索框会同步控制器中的过滤状态，不再出现实际已清空但输入框仍显示旧词的错位。
- 空列表提示优先区分“当前筛选无匹配项”和“目录为空/当前目录无数据库文件”。

本次验证记录：
- 真机在 `/data/user/0` 页面使用 `pica` 过滤后，能稳定定位 `com.github.pacalini.pica_comic`。
- 点进 `/data/user/0/com.github.pacalini.pica_comic` 后，搜索词会被自动清空。
- 真机复测确认：当前已可进入该目录，且在“仅显示数据库”开启时仍可列出目录项。
- 当前抓到的可见子目录包括 `app_flutter`、`app_textures`、`app_webview`、`cache`、`code_cache`、`databases`、`files`、`no_backup`、`shared_prefs`。

### V1.0.5 高级访问拦截与表格列宽拖拽

提交：`528e854 V1.0.5 收紧高级访问拦截并优化数据表交互`

本次改动重点：
- 高级访问能力改为“已授权/可用”和“是否启用参与访问链”分离；关闭 `Root` / `Shizuku` / `全部文件访问` 后，不再继续使用对应通道。
- `FileAccessController` 对 `forcedMode` 和最近打开回放增加前置校验；禁用后会直接阻断，而不是继续尝试受限路径。
- 首页最近打开和文件浏览测试路径的失败提示改成底部短提示，不再弹整段红色异常文本。
- 数据表页表头支持长按后水平拖动，动态调整字段列宽。
- 数据表主视图继续保留轻量列表渲染，列宽拖拽基于当前轻量表头/行布局实现。

本次验证记录：
- `flutter analyze`：通过，`No issues found!`
- `flutter build apk --debug`：通过
- 真机：`f294cd23 / Android 14 (API 34)`，已安装当前 debug APK
- 高级访问页当前验证状态：
  - `Root 模式 / Root 可用 / checked=false / 已授权，但当前不会使用此访问通道。`
  - `Shizuku 模式 / 已授权 / checked=false / 已授权，但当前不会使用此访问通道。`
- 首页最近打开验证：
  - 点击 Root 最近打开项后，底部提示 `Root 权限无/或未启用`
  - 点击 Shizuku 最近打开项后，底部提示 `Shizuku 权限无/或未启用`
- 文件浏览器验证：
  - 进入文件浏览器后，在 Root 已关闭状态点击 `Root 测试路径`，页面保持在当前目录，不再直接进入受限路径
  - 同时底部提示 `Root 权限无/或未启用`
- 相关真机取证产物：`flsqlite-root-recent.xml`、`flsqlite-shizuku-recent.xml`、`flsqlite-browser-rootchip.xml`、`flsqlite-access.xml`

### V1.0.6 Shizuku 存储目录回退与调试产物清理

提交：`821667f V1.0.6 修复 Shizuku 回退并清理调试产物`

本次改动重点：
- `FileAccessController` 将 `forcedMode: shizuku` 从“硬限制单通道”调整为“优先尝试后允许继续回退”；仅 `forcedMode: root` 继续保持严格模式。
- 文件浏览页的测试路径按钮不再因为 `Shizuku` 未启用而提前拦截；当 `Root` 可用时，`Shizuku 测试路径` 可继续回退到 `Root`。
- `/storage`、`/sdcard` 普通目录候选链补入 `Shizuku`：`normal → manageAllFiles → shizuku → root`，修复“普通/备份目录”在仅启用 `Shizuku` 时只能看到局部内容的问题。
- 清理并忽略弃用的本地抓图/XML 调试产物，避免它们继续作为已删除文件长期出现在提交列表中。

本次验证记录：
- `flutter analyze`：通过，`No issues found!`
- 当前回归结论：
  - `Shizuku 测试路径` 在 `Root` 可用时不再先弹 `Shizuku 权限无/或未启用`，而是允许继续按回退链访问。
  - `普通/备份目录` 在 `Shizuku` 授权下已恢复完整显示，不再停留在仅显示 `luluka` 的局部结果。
- 本轮未重新补抓新的 XML/截图产物；如后续还要保留这类本地取证文件，应放到未跟踪且被忽略的路径中。

### V1.0.7 Android 受限目录友好提示

提交：`c0122e9 V1.0.7 优化 Android 受限目录访问提示`

本次改动重点：
- `FileAccessController` 新增统一的 `FileAccessFailureException`，把目录列举失败和数据库打开失败收敛为“友好摘要 + 详细错误”的统一结构。
- 对 `/storage/emulated/0/Android`、`/storage/emulated/0/Android/data`、`/storage/emulated/0/Android/obb` 以及对应 `/sdcard/Android` 路径增加专门提示。
- 当当前访问链只有 `normal/manageAllFiles` 时，错误摘要会直接提示“该目录受 Android 限制，全部文件访问不足以访问，请启用 Shizuku 或 Root”。
- 当已经尝试过 `Shizuku` 或 `Root` 仍失败时，错误摘要会改成“该目录受 Android 限制，已尝试 Shizuku 或 Root，但当前仍无法访问”。
- 文件浏览页错误卡片和首页打开数据库失败的底部提示，都会优先展示这层友好摘要；详情中仍保留路径、模式链和底层异常，便于继续排查。

本次验证记录：
- `flutter analyze`：通过，`No issues found!`
- 代码层当前预期行为：
  - 进入 `Android/data` 时，不再优先显示整段 `Permission denied` / `PathAccessException`；摘要会直接说明“全部文件访问不足以访问，请启用 Shizuku 或 Root”。
  - 通过“全部文件访问”打开 `Shizuku 测试路径` 失败时，首页底部提示也会改成同类友好说明，而不是整段原始异常。
- 本轮未重新做真机复测；若继续验证，只需复点 `Android/data` 和 `Shizuku 测试路径`，确认摘要文案已切换即可。

### V1.0.8 数据表滚动交互与保存体验优化

提交：`5e148f5 V1.0.8 优化数据表保存与滚动交互`

本次改动重点：
- 数据表保存后改为在当前会话内静默刷新，不再出现像“退回后重新打开数据库”的视觉抖动。
- 行新增/编辑/删除后的刷新链路与主键定位逻辑一起收紧，避免保存成功但列表定位异常。
- 数据表顶部表名条改成基于滚动方向的渐隐/渐显状态机，而不是简单的二值显示。
- 表名条支持更长的收起距离、顶部下拉即时显示、松手后按阈值自动展开，以及已完全展开时继续下拉不再误触重新隐藏。
- 本轮继续补上向下隐藏释放分支：当隐藏进度达到 `1/4` 后松手，会自动缓慢补全到完全隐藏；相关阈值已提成显式常量，便于后续继续微调。
- 数据表列宽拖拽、滚动性能优化、键盘弹出底栏稳定、最近打开排序等近期交互改动保持兼容。

本次验证记录：
- `flutter analyze`：通过，`No issues found!`
- 静态检查确认本轮表名条释放阈值调整未引入新的 lint 或 analyze 问题。
- 当前手感预期：
  - 向上回拉显示表名条时，显示达到既定阈值后松手会自动展开。
  - 向下滚动隐藏表名条时，隐藏达到 `1/4` 后松手会自动补全隐藏。
  - 顶部已完全展开时继续下拉，不会再先隐藏再随着手势重新展开。


### V1.0.9 表切换记忆与最近打开 Root 恢复

提交：`14e3089 V1.0.9 修复最近打开 Root 恢复并优化表交互`

本次改动重点：
- 数据表顶部表名支持“再次点击当前表即关闭、再次点击重新打开”，点击其他表时直接切换。
- 为每个表在当前数据库会话内记录最近浏览页码；关闭表再打开，或多表来回切换时，会恢复到该表上次停留页。
- 数据表行操作补充颜色语义：删除按钮改为红色，编辑按钮改为橙色，保存按钮改为蓝色。
- 行详情页顶部新增删除按钮，行为与列表页删除一致：先确认，再删除当前行，并保留“待保存到源文件”的工作流。
- 首页启动时会主动刷新高级访问能力状态；点击“最近打开”前也会再次强制刷新，修复应用重装或重开后首页最近打开无法直接打开 Root 数据库的问题。

本次验证记录：
- `flutter analyze`：通过，`No issues found!`
- 代码层当前预期行为：
  - 同一表名可开关，重新打开后恢复到上次页码。
  - 行列表与行详情页新增/调整后的按钮颜色保持一致。
  - 应用冷启动后，不必先进入“Android 高级访问”页，首页“最近打开”的 Root 项也会先刷新状态再尝试打开。


### V1.1.0 最近打开增强与 Shizuku 保存修复

提交：`595a6b0 V1.1.0 增强最近打开并修复 Shizuku 保存`

本次改动重点：
- 修复 Shizuku 打开的数据库保存未生效问题：保存回写不再重新按路径推导访问模式，而是沿用当前数据库会话的实际 `accessMode` 回写源文件。
- 首页“最近打开”改为最多仅展示 4 条，点击标题整行可进入完整历史页。
- 首页最近打开卡片支持左右滑删除、长按删除，并修复滑动删除背景超出卡片圆角边界的问题。
- 新增完整“最近打开”页面：
  - 支持长按进入多选，选中项仅以淡蓝色背景表示，不显示复选框。
  - 右上角增加“多选/退出多选”和“清空/删除所选”按钮；非多选时可一键清空，多选时删除所选项。
- 首页与完整历史页的最近打开条目右侧信息布局统一：模式标签上移，其下显示“距离上一次打开”的相对时间，如 `刚刚 / 5分钟前 / 2小时前 / 3天前`。
- 历史记录状态层补充单项删除、批量删除和全部清空接口，便于主页与详情页共用。

本次验证记录：
- `flutter analyze`：通过，`No issues found!`
- 代码层当前预期行为：
  - Shizuku 打开的 `Android/data` 数据库保存后，会按 Shizuku 通道回写。
  - 首页最近打开只保留 4 条预览，更多历史通过标题进入完整历史页查看。
  - 最近打开条目在主页与完整历史页均可删除；完整历史页还支持长按多选与批量删除。
  - 右侧时间显示为相对时间，不再显示固定时间戳。

### V1.1.1 设置页与中英文本地化

提交：`10c4fe3 V1.1.1 添加设置页并接入中英文本地化`

本次改动重点：
- 首页左上角标题移除，改为设置按钮；点击后进入新设置页。
- 新增 `AppSettingsController` 语言配置持久化能力，支持保存当前语言选择，并在应用重启后恢复。
- 应用增加中英文本地化支持，当前接入 `中文 / English` 两种语言。
- 设置页增加语言切换选项，并改为不触发弃用告警的分段选择控件实现。
- `MaterialApp` 已接入 `locale`、`supportedLocales` 和 Flutter 本地化委托，语言切换会直接驱动界面刷新。
- 主页、最近打开、Android 高级访问、文件浏览器、数据表、行详情/编辑、通用访问控件等关键页面文案已切换到统一的 `AppStrings` 资源。
- 额外清理本轮 8 条 `flutter analyze` 提示，包括：
  - 设置页弃用 API 提示
  - `AppStrings` 无用 import
  - 字符串插值样式提示
  - 首页最近打开打开流程中的 `BuildContext` 异步使用提示

本次验证记录：
- `flutter analyze`：通过，`No issues found!`
- `flutter build apk`：通过
- 真机安装：通过，目标设备 `22127RK46C / f294cd23 / Android 14 (API 34)`
- 当前代码层预期行为：
  - 首页左上角点击设置按钮可进入设置页。
  - 设置页切换语言后，当前界面文案会立即切换，并在重启应用后保持上次选择。
  - 中文与英文模式下，主页和关键功能页均会显示对应语言文案。

### V1.1.2 Windows 桌面打开体验与应用图标

提交：`c9b889b V1.1.2 优化 Windows 桌面打开体验`

本次改动重点：
- Windows/桌面端文件浏览器默认进入当前用户“文档”目录，不再默认进入 Android 存储路径。
- 桌面端文件浏览器顶部快捷入口改为本机盘符列表；Android 端继续保留 Root/Shizuku/普通目录测试路径入口。
- 桌面端首页底部说明简化为 `支持直接拖入或手动打开`；Android 端仍显示“普通目录访问 / 全部文件访问 / Root / Shizuku”访问方式列表。
- Windows 端支持拖拽数据库文件到窗口直接打开，且支持命令行文件路径参数，用于“打开方式”或外部调用。
- 当当前数据库存在未保存修改时，拖入/打开新文件会弹出“是否保存当前修改，然后打开新文件？”确认；选择“是”会先保存当前库，再打开新文件，选择“否”会放弃当前未保存修改并打开新文件。
- 数据库工作副本路径改为每次打开/刷新都生成唯一文件名，避免 Windows 下刷新时因当前工作副本仍被占用而复制覆盖失败。
- Windows runner 启动时会在当前用户级注册 `.db`、`.sqlite`、`.sqlite3` 的 Open With 能力，让 `FlSQLite Viewer` 出现在 Windows“打开方式”候选列表中；该注册不强行抢默认打开程序。
- 暂定 `design/icon_drafts/icon_16_sql_panel_search_white.svg` 为应用主图标，并生成 Android 各密度 `ic_launcher.png`、Windows `app_icon.ico` 和 `icon_16_app_icon_preview.png` 预览图。
- `design/icon_drafts/` 中保留本轮 SVG 图标草稿和 `preview.html`，便于后续继续挑选或调整主图标。

本次验证记录：
- `dart format`：已格式化本轮 Dart 改动文件。
- `ReadLints`：本轮关键 Dart/runner 文件未发现新增诊断问题。
- `flutter build windows`：通过，产物为 `build\windows\x64\runner\Release\flsqliteviewer.exe`。
- 图标资源格式检查通过：Android PNG 覆盖 `48/72/96/144/192` 密度，Windows ICO 可读。
- Windows Open With 注册逻辑需要先运行一次新版 `flsqliteviewer.exe`，之后系统“打开方式”列表才会刷新出 `FlSQLite Viewer`。

### V1.1.3 Windows 无边框窗口管理

提交：`4285695 V1.1.3 接入 Windows 无边框窗口管理`

本次改动重点：
- Windows 桌面端接入无边框窗口体验，并移除系统默认标题栏。
- 改用 `window_manager` 处理 Flutter 侧标题栏拖动、窗口最小化、最大化/还原和关闭控制，避免手写 Windows 命中测试带来的拖动/拉伸冲突。
- 保留 Windows 原生窗口可调整大小能力，窗口边缘拉伸由系统窗口样式负责。
- 桌面端标题栏和页面布局与当前主题保持一致，避免 Windows 端出现双标题栏。

本次验证记录：
- `flutter build windows`：通过。
- 代码层当前预期行为：
  - Windows 窗口顶部自定义区域可拖动窗口。
  - 最小化、最大化/还原、关闭按钮可正常工作。
  - 窗口边缘可继续拉伸调整大小。

### V1.1.4 Android/Windows 图标与 Release 配置

提交：`4f79eaa V1.1.4 更新 Android 应用图标`

本次改动重点：
- Android Manifest 已切换到新生成的 `@mipmap/ic_launcher_sql` / `@mipmap/ic_launcher_sql_round` 图标资源。
- Android 各密度启动图标资源已使用 `design/icon_drafts` 中选定的 SQL 面板放大镜图标方案。
- Flutter 应用版本号同步为 `1.1.4+4`。
- Android release 构建改为读取 `android/key.properties` 中的签名信息，不再使用 debug 签名生成 release 包。
- Windows 端 `windows/runner/resources/app_icon.ico` 已用 `design/icon_drafts/icon_16_sql_panel_search_white_square.png` 重新生成。
- `build/` 目录和构建产物不进入提交范围。

本次验证记录：
- `flutter build apk --debug`：通过，已验证 Android 图标资源可参与构建。
- `flutter build apk --release`：通过；此前失败原因为本机 Gradle 8.14 transforms 缓存损坏，清理缓存后恢复。
- `flutter build windows`：通过，已验证 Windows 新 `.ico` 可参与构建。
- Windows ICO 检查：资源文件可读，已由指定 PNG 重新生成。
- 当前待提交补充项：release 签名配置、Windows 图标更新、本文档记录；提交说明继续使用 `V1.1.4`，不递增提交版本号。

### V1.1.5 Shizuku Android/data 枚举补全与目录统计

提交：待本轮提交

本次改动重点：
- Android 端新增 Shizuku AIDL + UserService：
  - 新增 `android/app/src/main/aidl/lingxue/flsqliteviewer/shizuku/IFlSqlViewerShizukuFileService.aidl`
  - 新增 `android/app/src/main/kotlin/lingxue/flsqliteviewer/shizuku/FlSqlViewerShizukuFileService.kt`
- Shizuku 目录枚举不再只依赖 `Shizuku.newProcess + shell ls`；现在优先通过 UserService 直读目录，并在 `/storage/emulated/0/Android/data` 根路径下合并已安装包名列表与实际目录项，尽量补齐 MIUI / FUSE 环境下漏掉的包目录。
- `MainActivity.kt` 已接入 Shizuku UserService 的绑定、断开、Binder 存活判断与失败回退；`list/read/write/exists` 现在优先走 UserService，失败时才回退原有 shell 通道。
- Root 访问链已补 `/storage/emulated/0` → `/data/media/0` 的真实共享存储路径翻译；Root 浏览共享存储和 `Android/data` 时，不再只走 FUSE 视图。
- 文件浏览器顶部在 Shizuku 且当前位于 `Android/data` 根路径时，会显示一条 FUSE 限制提示，明确“可能少 1-2 项，完整结果需要 Root”。
- 文件浏览器在“仅显示数据库”右侧新增当前目录数量统计，文案为 `N 个文件夹 · N 个文件`。
- 数量统计在手机窄屏下已改为按可用宽度自动缩放，避免被截断成省略号。
- `FileBrowserController` 已补 `directoryCount` / `fileCount`，供页面直接复用当前过滤后的统计口径。
- Android `build.gradle.kts` 已开启 `buildFeatures { aidl = true }`，用于生成本轮新增的 AIDL 接口。

本次验证记录：
- `flutter analyze`：通过，`No issues found!`
- `flutter analyze lib/features/file_browser/presentation/file_browser_page.dart`：通过，数量布局调整未引入回归
- `.\android\gradlew.bat :app:compileDebugKotlin`：通过，AIDL 生成与 Kotlin 编译正常
- 当前代码层预期行为：
  - `Shizuku 测试路径` 进入 `/storage/emulated/0/Android/data` 时，目录项会明显比原先 shell 枚举更完整。
  - 当前位于 `Android/data` 根路径且使用 Shizuku 时，页面顶部会显示 FUSE 提示条。
  - “仅显示数据库”右侧会显示当前目录的文件夹/文件数量，且在手机窄屏下仍可完整显示。
  - Root 模式进入共享存储路径时，会优先按 `/data/media/0` 真实路径访问。

## 8. 提交注意事项

- 提交前执行 `git status`、`git diff`、`git log` 检查变更范围
- 只提交源码和文档，排除 `build/`、`.dart_tool/`、`local.properties` 等构建产物和缓存
- 提交信息格式：版本号 + 简明目的，如 `V1.0.0 初始项目框架`
- 使用 `flutter analyze` 确认 0 issues 后再提交
- .gitignore 应包含：`build/`、`.dart_tool/`、`*.iml`、`local.properties`、`android/app/build/`